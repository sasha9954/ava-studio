# AVA Loading Card Glass Final Polish

UI-only CSS patch.

What it changes:
- removes the visible rectangular loading-page backdrop more aggressively;
- makes the actual loading card glass/transparent, not just the page behind it;
- keeps text readable;
- applies to Board/Timing/Assembly loading screens;
- no backend, persistence, asset, credit, or route logic changes.
